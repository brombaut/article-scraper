name: Publish new version to NPM
on:
  workflow_dispatch:
    inputs:
      version_update_type:
        description: "SemVer update version type (patch, minor, or major)"
        required: true
        default: "patch"

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Hello ${{ github.event.inputs.version_update_type }}!"
